version: 2.1

jobs:
  build:
    docker:
      - image: alpine:3.18
    steps:
      - run: echo "Building the project"

  continue-workflow:
    docker:
      - image: alpine:3.18
    steps:
      - run: echo "Continuing with the next steps in the workflow..."

workflows:
  version: 2
  run-build:
    jobs:
      - build
      - approve-step:
          type: approval
          name: "Approval Required: Review Information"
          requires:
            - build
      - continue-workflow:
          requires:
            - approve-step
